# Lightweight install: everything needed to run the offline mock demo, CLI,
# API, and full test suite — WITHOUT heavy ML (no torch / sentence-transformers
# / chromadb / transformers). The hashing embedder + numpy store make RAG fully
# functional with just these.
anthropic>=0.40.0
httpx>=0.27.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
numpy>=1.24.0
rich>=13.0.0
sse-starlette>=2.0.0
python-dotenv>=1.0.0
pypdf>=4.0.0
python-multipart>=0.0.9
pytest>=8.0.0

# Hardening runtime (torch-free) — the persistence/migrations layer and its
# tests need these on the light/CI path:
sqlalchemy>=2.0.0
alembic>=1.13.0
prometheus-client>=0.20.0
structlog>=24.1.0
tenacity>=8.2.0

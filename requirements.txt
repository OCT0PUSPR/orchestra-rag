# Full runtime dependencies for orchestra-rag.
# Core (light) — required for the offline mock demo, CLI, API, and tests:
anthropic>=0.40.0
httpx>=0.27.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
numpy>=1.24.0
rich>=13.0.0
sse-starlette>=2.0.0
python-dotenv>=1.0.0
pypdf>=4.0.0
python-multipart>=0.0.9

# Heavy / optional ML — only needed for real embeddings or chroma:
# install with `pip install -r requirements.txt` to get everything, or skip
# these for a lightweight install via requirements-min.txt.
sentence-transformers>=2.6.0
chromadb>=0.4.24
transformers>=4.40.0
torch>=2.2.0
